{
  "name": "@agtm/deploy",
  "version": "1.1.1",
  "description": "Ferramentas para deploy interativo",
  "main": "index.js",
  "scripts": {
    "publish-patch": "npm version patch && npm publish --access public && git push",
    "publish-minor": "npm version minor && npm publish --access public && git push",
    "publish-major": "npm version major && npm publish --access public && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "deploy": "./deploy.mjs"
  },
  "author": "André Timermann",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "cli-table3": "^0.6.5",
    "enquirer": "^2.4.1",
    "kleur": "^4.1.5",
    "node-fetch": "^3.3.2"
  },
  "files": [
    "src",
    "deploy.mjs"
  ]
}
